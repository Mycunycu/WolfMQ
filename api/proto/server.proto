syntax = "proto3";

package api;
option go_package = "/server";

service Server {
  rpc DoSomething(Request) returns (Response) {}
}

message Request {
  string message = 1;
}

message Response {
  string message = 1;
}